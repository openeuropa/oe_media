media.source.javascript_asset:
  type: media.source.field_aware
  label: 'JavaScript asset media source configuration'
  mapping:
    thumbnails_directory:
      type: uri
      label: 'URI of thumbnail storage directory'

oe_media_js_asset.settings:
  type: config_object
  label: 'Environment allowed options'
  mapping:
    environments:
      type: sequence
      label: 'Allowed environments'
      sequence:
        type: mapping
        label: 'Environment ID'
        mapping:
          label:
            type: string
            label: 'Environment option label'
          url:
            type: string
            label: 'Environment URL'
